FROM ubuntu

LABEL org.opencontainers.image.source = "https://github.com/vivek2021-dot/Web-App.git"

WORKDIR /app
COPY $srcDir/app ./app

RUN apt-get update 
RUN apt-get install -y apache2 

EXPOSE 5000

CMD ["apache2ctl", "-D", "FOREGROUND"]  
 
